# Alternativa con ntfy.sh - no ejecutar, solo referencia
      - name: Send push notification
        if: always()
        run: |
          STATUS="${{ job.status == 'success' && '✅ ÉXITO' || '❌ FALLO' }}"
          MESSAGE="${{ job.status == 'success' && 'Depósito ejecutado correctamente' || 'Depósito falló' }}"
          
          curl -d "🏦 Casa Ortega: $STATUS - $MESSAGE" \
            -H "Title: Automated Payments" \
            -H "Priority: high" \
            ntfy.sh/tu-topic-unico-aqui
