{
  "name": "casa-ortega-automation",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:e2e": "npx playwright test scripts/deposit-wallet.spec.ts --project=chromium",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^17.2.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
